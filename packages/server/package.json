{
  "name": "@findeth/server",
  "description": "A tool to help you find your lost Ethereum address, Ether or tokens, by searching through a bunch of derivation paths",
  "version": "1.1.2",
  "license": "MIT",
  "author": "Maarten Zuidhoorn <maarten@zuidhoorn.com>",
  "scripts": {
    "serve": "cross-env IS_LOCAL=true ts-node --files src",
    "lint": "yarn run lint:ts && yarn run lint:tslint",
    "lint:ts": "tsc --project . --noEmit",
    "lint:tslint": "tslint --project .",
    "prettier": "prettier --write --config ../../.prettierrc --list-different './**/*.ts?(x)'"
  },
  "dependencies": {
    "finalhandler": "^1.1.2",
    "ora": "^3.4.0",
    "selfsigned": "^1.10.4",
    "serve-static": "^1.14.1"
  }
}
