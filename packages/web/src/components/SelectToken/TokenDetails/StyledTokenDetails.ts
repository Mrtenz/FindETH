import styled from 'styled-components';
import { List } from '@mycrypto/ui';

export const TokenList = styled(List)`
  margin: 18px 0 0 0;
  padding: 0;

  dl:last-child {
    margin-bottom: 0;
  }
`;
