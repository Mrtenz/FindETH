import styled from 'styled-components';

// Monospace font as defined by MyCrypto
const MONOSPACE_FONT = `'Roboto Mono', Menlo, Monaco, Consolas, 'Courier New', monospace`;

const Code = styled.span`
  font-family: ${MONOSPACE_FONT};
`;

export default Code;
